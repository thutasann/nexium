import { add } from '../build/Release/nexium.node'

/** Add Two Numberic Numbers */
export const addTwoNumericNumbers = (a: number, b: number): number => {
  return add(a, b)
}

/** Add Two Numbers */
export const addTwoNumbers = (a: number, b: number): number => {
  return add(a, b)
}
